// const add = (a, b) => a + b;

function add (a, b) {
    return a + b;
}

// const subtract = (a, b) => a - b;
//
// const multiply = (a, b) => a * b;
//
// const divide = (a, b) => a / b;
//
// const modulo = (a, b) => a % b;

exports = {
    add,
    // subtract,
    // multiply,
    // divide,
    // modulo,
};
