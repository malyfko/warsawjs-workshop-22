const { foo, bar } = require('../../../src');

describe('', () => {
    it('', () => {
        const f = foo();
        expect(f).toBe('foo');
    });

    it('', () => {
        const b = bar();
        expect(b).toBe('bar');
    });
});
